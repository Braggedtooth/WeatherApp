import Box from '@material-ui/core/Box'
import React from 'react'


const Wrapper = (props) => {
    return (
        <Box>
            {props.children}
        </Box>

    )

}

export default Wrapper